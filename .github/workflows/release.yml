name: Release (semantic-release)

on:
  push:
    branches: [master]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  release:
    runs-on: self-hosted
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          persist-credentials: true
          fetch-depth: 0

      - name: Check for in-progress runs
        run: |
          echo "🚀 Starting release workflow for ${{ github.ref }}"
          echo "📦 Package: n8n-nodes-easy-ai-agent"
          echo "🔄 Concurrency: Previous runs will be cancelled"

      - name: Install dependencies
        run: npm ci

      - name: Run linting
        run: npm run lint

      - name: Run tests (optional)
        run: npm test || echo "Tests skipped - external services not available in CI"
        continue-on-error: true
        env:
          CI: true

      - name: Setup npm authentication
        run: |
          # Create .npmrc with proper authentication
          echo "//registry.npmjs.org/:_authToken=${{ secrets.NPM_TOKEN }}" > .npmrc
          echo "registry=https://registry.npmjs.org/" >> .npmrc
          echo "always-auth=true" >> .npmrc
          
          # Verify npm authentication
          npm whoami
          
          # Show npm configuration
          npm config list
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Run semantic-release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
          NODE_ENV: production
        run: npx semantic-release
